{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "//installCommand": "--include=dev is required: NODE_ENV=production makes npm skip devDependencies, which would leave the Nest CLI missing and produce no dist/ to run.",
    "installCommand": "npm ci --include=dev",
    "buildCommand": "npm run build --workspace=apps/api"
  },
  "deploy": {
    "startCommand": "npm run start:prod --workspace=apps/api",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
